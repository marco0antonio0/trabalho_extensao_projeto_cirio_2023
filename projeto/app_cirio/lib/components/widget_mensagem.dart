import 'package:flutter/material.dart';

class Widgedmensage extends StatelessWidget {
  const Widgedmensage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
